apiVersion: v2
name: gamedex
description: Gamedex — searchable browser for a Dropbox-hosted games spreadsheet
type: application
version: 0.24.0
# appVersion tracks the gamedex image tag in values.yaml (image.tag).
appVersion: "0.24.0"
maintainers:
  - name: zachd
