apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ffxiv-1x.fullname" . }}-data
  labels:
    {{- include "ffxiv-1x.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.persistence.data.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.data.size }}
