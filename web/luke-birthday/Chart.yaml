apiVersion: v2
name: luke-birthday
description: luke.diemer.codes — a one-off GeoCities-style 30th birthday page for Luke, baked into nginx
type: application
version: 1.5.1
# appVersion tracks the image tag in values.yaml (image.tag).
appVersion: "1.5.1"
maintainers:
  - name: zachd
