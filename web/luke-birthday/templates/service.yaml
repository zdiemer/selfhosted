apiVersion: v1
kind: Service
metadata:
  name: {{ include "luke-birthday.fullname" . }}
  labels:
    {{- include "luke-birthday.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "luke-birthday.selectorLabels" . | nindent 4 }}
