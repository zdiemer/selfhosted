apiVersion: v1
kind: Service
metadata:
  name: {{ include "kelsey-green.fullname" . }}
  labels:
    {{- include "kelsey-green.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kelsey-green.selectorLabels" . | nindent 4 }}
