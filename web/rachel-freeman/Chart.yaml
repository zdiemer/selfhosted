apiVersion: v2
name: rachel-freeman
description: Rachel Freeman's art store — Payload CMS admin + Next.js storefront + Postgres, selling originals and prints through Stripe Checkout
type: application
version: 0.13.1
# appVersion tracks image.tag in values.yaml. Both move together; build.sh warns
# if they drift.
appVersion: "0.13.1"
sources:
  - https://github.com/zdiemer/rachelfreeman
maintainers:
  - name: zachd
