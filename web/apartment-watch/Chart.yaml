apiVersion: v2
name: apartment-watch
description: Daily SF rental scraper that texts a digest of new matching listings via sms-relay
type: application
version: 0.1.0
# appVersion tracks the image tag in values.yaml (image.tag).
appVersion: "v1"
maintainers:
  - name: zachd
