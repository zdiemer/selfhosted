.venv/
src/
data/
Dockerfile
.dockerignore
build.sh
upgrade.sh
*.md
values.local.yaml
criteria.example.yaml
__pycache__/
*.pyc
# NOT criteria.yaml — templates/configmap.yaml reads it with .Files.Get, and
# anything listed here is invisible to that.

# 1Password reference templates — never part of the packaged chart.
values.local.tpl.yaml
criteria.tpl.yaml
