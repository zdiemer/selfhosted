# Pinned for reproducible image builds — bump deliberately.
#
# Everything here has a manylinux wheel, so the image needs no build toolchain.
# Note what is NOT here: shapely. The only geometry operation is point-in-polygon,
# which is ~30 lines in geo.py, and shapely would drag GEOS into the image for it.
#
# camoufox[geoip] pulls playwright and a ~50MB MaxMind database; the browser
# itself is fetched separately in the Dockerfile (`python -m camoufox fetch`).
camoufox[geoip]==0.5.5
httpx==0.28.1
beautifulsoup4==4.15.0
PyYAML==6.0.3

# The run-page web view (src/web.py), served from the same image.
fastapi==0.141.1
uvicorn==0.52.1
