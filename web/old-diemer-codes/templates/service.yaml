apiVersion: v1
kind: Service
metadata:
  name: {{ include "old-diemer-codes.fullname" . }}
  labels:
    {{- include "old-diemer-codes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "old-diemer-codes.selectorLabels" . | nindent 4 }}
