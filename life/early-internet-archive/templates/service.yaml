apiVersion: v1
kind: Service
metadata:
  name: {{ include "early-internet-archive.fullname" . }}
  labels:
    {{- include "early-internet-archive.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "early-internet-archive.selectorLabels" . | nindent 4 }}
