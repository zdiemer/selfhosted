apiVersion: v2
name: early-internet-archive
description: A read-only, provenance-first browser for Zach Diemer's personal web archive
type: application
version: 0.1.2
# appVersion tracks image.tag in values.yaml; build.sh enforces the match.
appVersion: "0.1.2"
maintainers:
  - name: zachd
