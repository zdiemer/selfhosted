apiVersion: v1
kind: Service
metadata:
  name: {{ include "carson.fullname" . }}
  labels:
    {{- include "carson.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "carson.selectorLabels" . | nindent 4 }}
