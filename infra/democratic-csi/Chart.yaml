apiVersion: v2
name: democratic-csi
description: TrueNAS-backed dynamic storage — iSCSI zvols for RWO, NFS datasets for RWX. Deployed twice, once per driver.
type: application
version: 1.0.0
appVersion: "1.9.3"
maintainers:
  - name: zachd
dependencies:
  - name: democratic-csi
    version: 0.15.1
    repository: https://democratic-csi.github.io/charts/
