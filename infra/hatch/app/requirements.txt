# Pinned, and manylinux wheels only — the image carries no build toolchain.
fastapi==0.118.0
uvicorn==0.52.4
httpx==0.28.1
# Tracks the k8s 1.34 line, which is what this cluster runs (scripts/k3s).
# The deciding reason for the official client over raw httpx is the token: the
# ServiceAccount token is a projected bound token that kubelet rotates, and
# load_incluster_config installs the refresh hook. A hand-rolled client that
# reads the file once works perfectly and then starts 401-ing months later.
kubernetes==34.1.0
